{
  "name": "grog",
  "private": true,
  "workspaces": [
    "shared",
    "api",
    "agent",
    "app"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "dev:agent": "yarn workspace @grog/agent dev",
    "dev:api": "yarn workspace @grog/api dev",
    "dev:app": "yarn workspace @grog/app dev"
  }
}
