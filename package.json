{
  "name": "grog",
  "private": true,
  "workspaces": [
    "shared",
    "web",
    "agent"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "dev:web": "yarn workspace @grog/web dev",
    "dev:agent": "yarn workspace @grog/agent dev"
  }
}
